import string 
import random
class Password:
    def password_gen(self):
        alphabet = string.printable  
        self.pswd = ''.join(random.choice(alphabet) for index in range(20))
        return self.pswd
    def set_name(self, enter_name):
        self.name = enter_name




